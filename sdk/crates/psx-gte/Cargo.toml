[package]
name         = "psx-gte"
version      = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description  = "PS1 GTE (COP2) inline-asm wrappers + fixed-point math types."

[lints.rust]
missing_docs = "warn"
