[package]
name         = "psx-gpu"
version      = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description  = "High-level PS1 GPU API: init, primitives, framebuffers."

[dependencies]
psx-hw = { workspace = true }
psx-io = { path = "../psx-io" }

[lints.rust]
missing_docs = "warn"
