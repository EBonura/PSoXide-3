[package]
name         = "psx-pad"
version      = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description  = "SIO0 controller-poll helpers (digital pad protocol)."

[dependencies]
psx-hw = { workspace = true }
psx-io = { path = "../psx-io" }

[lints.rust]
missing_docs = "warn"
