[package]
name         = "psx-vram"
version      = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description  = "Typed VRAM layout primitives for PS1 SDK: color, rect, tpage, CLUT, upload helpers."

[dependencies]
psx-hw = { workspace = true }
psx-io = { path = "../psx-io" }

[lints.rust]
missing_docs = "warn"
