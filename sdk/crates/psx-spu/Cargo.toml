[package]
name         = "psx-spu"
version      = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description  = "High-level PS1 SPU (Sound Processing Unit) API: typed voices, volume, pitch, ADSR, ADPCM sample upload, key-on / key-off."

[dependencies]
psx-hw = { workspace = true }
psx-io = { path = "../psx-io" }

[lints.rust]
missing_docs = "warn"
